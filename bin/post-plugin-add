#!/usr/bin/env bash
# Creates the "auto" marker dir so mise considers the version installed
# without requiring a separate `mise install` step.
set -euo pipefail

MISE_INSTALLS_DIR="${MISE_DATA_DIR:-$HOME/.local/share/mise}/installs/java-home"
mkdir -p "$MISE_INSTALLS_DIR/auto"
